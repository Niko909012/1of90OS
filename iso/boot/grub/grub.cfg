set timeout=5
set default=0

# Enable serial console so GRUB and the kernel can be observed via COM1
serial --unit=0 --speed=38400
terminal_input serial
terminal_output serial

menuentry "1of90OS" {
    multiboot /boot/stub.bin
    module /boot/kernel.raw
    boot
}
